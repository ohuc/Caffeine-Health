<?xml version="1.0" encoding="utf-8"?><!--
   Pre-API-31 counterpart of data_extraction_rules.xml (minSdk is 33, so this file is
   effectively unused, but kept consistent): never back up the auth DataStore — it holds
   the Home Assistant access/refresh tokens.
-->
<full-backup-content>
    <exclude domain="file" path="datastore/auth.preferences_pb" />
</full-backup-content>
