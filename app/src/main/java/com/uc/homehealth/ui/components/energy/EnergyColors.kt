package com.uc.homehealth.ui.components.energy

import androidx.compose.ui.graphics.Color

// The sun's own colors — content, not UI chrome: the sun stays gold in any theme
// (the irradiance halo drawn with these is the sanctioned glow exception).
// Shared by the hero arc and the sundial so the instrument family keeps one gold.
internal val SunGold = Color(0xFFFFC65A)
internal val SunAmber = Color(0xFFFFB020)
